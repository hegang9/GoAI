# 精读顺序

本清单面向**逐行精读源码**，按「从易到难」组织：先读无依赖的叶子包（工具、领域），再读把它们串起来的应用与接口层，最后读最复杂的基础设施适配器与组合根。每个文件标注了**行数**与**精读重点**，方便边读边勾选、按需深入。

> 架构分层：`pkg`（通用工具）→ `internal/domain`（领域）→ `internal/application`（应用）→ `internal/interfaces/http`（接口）→ `internal/infrastructure`（基础设施）→ `internal/bootstrap`（组合根）→ `cmd/server`（入口）。
>
> 依赖方向统一指向领域层：`interfaces → application → domain`、`infrastructure → domain`（实现端口），`bootstrap` 负责注入。
>
> **导航建议**：正式精读前可先花 5 分钟快速扫一遍 `internal/bootstrap/app.go`（240 行），它像一张「装配地图」，自上而下把所有组件连了起来；看不懂细节没关系，只需建立「谁依赖谁」的印象，后面每读到一个组件都能在这张图里定位。

---

## 阶段 0：文档与全局认知（最易）

- [x] `README.md` — 项目目标、技术栈、目录结构、分层规则、启动与关闭流程
- [x] `docs/API.md` — 系统对外能力（用户、聊天、流式、文件、图片、TTS）
- [x] `架构优化方案.md` — 为什么这样分层（背景、目标、端口与适配器）
- [x] `TODO.md` — 已落地的重构与后续方向

---

## 阶段 1：`pkg/` 通用工具（叶子包，无业务依赖，最易）

按行数从小到大读，建立「这些是纯技术工具、不含业务」的认知：

- [x] `pkg/id/uuid.go`（9） — UUID 生成
- [x] `pkg/hash/md5.go`（17） — MD5 摘要（注意：仅用于非密码场景）
- [x] `pkg/random/code.go`（20） — 随机数字验证码生成
- [x] `pkg/fileutil/file.go`（110） — 上传文件校验（`.md`/`.txt`/`.pdf`/`.docx` 扩展名白名单）、目录清理、路径逃逸防护
- [x] `pkg/code/code.go`（116） — 业务状态码定义与 HTTP 状态码映射，**精读重点**：错误码如何贯穿全项目响应
- [x] `pkg/logger/logger.go`（92） — 结构化日志门面，**精读重点**：`LOG_LEVEL` 控制、各 `Info/Error/Fatal` 用法

---

## 阶段 2：`internal/domain/` 领域层（纯净、定义全项目词汇表，易但关键）

领域层只有类型与接口、几乎无第三方依赖，是理解整个系统的「字典」。先读端口与值对象，再读聚合与领域服务。

### 各领域端口（小而集中）

- [x] `internal/domain/image/ports.go`（10） — `Recognizer` 端口
- [x] `internal/domain/rag/ports.go`（18） — `Indexer` 端口（多文档语义：`Index`/`Delete` 均按 `accountNo` + `storedName` 操作）
- [x] `internal/domain/storage/ports.go`（24） — `DocStorage` 端口（多文档知识库：`ListUserDocs`/`Save`/`Remove`/`RemoveUserDoc`/`ClearUserDocs`）
- [x] `internal/domain/tts/ports.go`（24） — `Synthesizer` 端口与 `TaskResult`

### 用户领域

- [x] `internal/domain/user/user.go`（74） — `User` 实体、`ErrNotFound`，以及 `Repository`/`PasswordHasher`/`TokenIssuer`/`CaptchaStore`/`Mailer` 端口。**精读重点**：领域如何用端口表达「我需要什么」而不关心「怎么实现」

### 会话领域（项目核心，按此顺序读）

- [x] `internal/domain/chat/message.go`（29） — `Message`/`Session` 值对象，注意 `IsUser`、`AccountNo`、`SessionID` 字段含义
- [x] `internal/domain/chat/ports.go`（51） — **精读重点**：`Model`/`ModelFactory`（屏蔽 OpenAI/RAG/MCP/Ollama 差异）、`MessageSink`（异步持久化，屏蔽 MQ）、`MessageRepository`/`SessionRepository`、`StreamCallback`
- [x] `internal/domain/chat/conversation.go`（104） — `Conversation` 聚合，**精读重点**：`Generate`/`Stream` 如何先追加用户消息→调用 `Model`→追加 AI 回复→通过 `Sink` 持久化；`AddMessage(persist)` 在回放时为何传 `false`
- [x] `internal/domain/chat/manager.go`（70） — `Manager` 领域服务，**精读重点**：`账号 → 会话 → Conversation` 三级映射、`GetOrCreate` 复用模型实例、`sync.RWMutex` 并发保护

> 读完阶段 2 应能回答：领域层为什么不依赖 gin/gorm/eino？端口接口由谁实现？（答案在 `test/architecture_test.go` 与基础设施层）

---

## 阶段 3：接口层基础设施（薄壳，易）

先读与具体业务无关的「HTTP 公共件」，它们决定了所有接口的统一形态：

- [x] `internal/interfaces/http/dto/common.go`（28） — 通用响应/分页结构
- [x] `internal/interfaces/http/dto/user.go`（30）、`session.go`（49）、`tts.go`（17）、`file.go`（24，上传/列出/删除 RAG 文档）、`image.go`（6） — 各接口请求/响应 DTO
- [x] `internal/interfaces/http/httpx/response.go`（59） — **精读重点**：泛型 `BindJSON[T]`、统一 `JSON`、路由包装 `Handler[T]`（让处理器直接声明类型化 DTO）
- [x] `internal/interfaces/http/sse/writer.go`（67） — **精读重点**：SSE 适配器如何把 `Chunk()` 回调编码为 `data:` 帧、首帧 `sessionId`、`[DONE]` 结束帧
- [x] `internal/interfaces/http/middleware/jwt.go`（54） — **精读重点**：Token 来源（Header / `?token=`）、失败响应、用户信息如何注入 `gin.Context`；注意它依赖 `httpx`、`TokenIssuer`，不反向依赖 controller

---

## 阶段 4：`internal/application/` 应用层（用例编排，中等）

应用层只依赖领域端口，是「业务流程」所在。从最简单的用例读到最复杂的会话用例：

- [x] `internal/application/image/service.go`（30） — 最简用例：调用 `Recognizer` 端口
- [x] `internal/application/tts/service.go`（43） — 调用 `Synthesizer` 端口
- [x] `internal/application/file/service.go`（132） — **精读重点**：上传（落盘 + 分块建向量索引，多文档追加不清旧）、列出（`ListRagFiles`）、批量删除（`DeleteRagFiles`，尽力而为、去重、部分成功语义）
- [x] `internal/application/chat/service.go`（150） — **精读重点**：创建会话/发消息/流式/历史查询如何编排 `Manager` 与 `SessionRepository`；流式回调如何从接口层透传到领域
- [x] `internal/application/user/service.go`（207，本阶段最难） — **精读重点**：注册（验证码校验 + `AccountNo` 唯一性重试）、登录（`PasswordHasher` 校验 + `TokenIssuer` 签发）、发送验证码（`Mailer` + `CaptchaStore`）

---

## 阶段 5：接口层控制器与路由（把前面串起来，中等）

读完应用层后，回到接口层看请求如何落到用例：

- [x] `internal/interfaces/http/controller/handlers.go`（33） — `Handlers` 结构体如何聚合各应用服务（依赖注入入口）
- [x] `internal/interfaces/http/controller/user.go`（26）、`tts.go`（34）、`file.go`（73，上传/列出/删除 RAG 文档）、`image.go`（41） — 薄控制器：绑定 DTO → 调用服务 → 统一响应
- [x] `internal/interfaces/http/controller/chat.go`（105） — **精读重点**：会话列表/创建发消息/流式/历史，流式接口如何创建 SSE writer 并把 `Chunk()` 作为回调传入应用服务
- [x] `internal/interfaces/http/router/router.go`（84） — **精读重点**：路由分组、公开/受保护路由、JWT 中间件挂载、`httpx.Handler` 注册方式；`/file` 组含上传/列出/删除三个 RAG 文档接口

> 读完阶段 5 应能完整回答：一个用户请求从路由 → 中间件 → 控制器 → 应用服务 → 领域端口的全过程。

---

## 阶段 6：`internal/infrastructure/` 基础设施层（适配器实现，较难）

这里是端口的具体实现，含较多技术细节。按「易 → 难」「先存储后 AI」顺序读：

### 持久化（MySQL + GORM）

- [x] `internal/infrastructure/persistence/po.go`（73） — 持久化对象（PO）与表结构映射，注意 PO 与领域实体/值对象的区别
- [x] `internal/infrastructure/persistence/db.go`（85） — **精读重点**：连接构建、连接池、`AutoMigrate`
- [x] `internal/infrastructure/persistence/session_repo.go`（48）、`message_repo.go`（53）、`user_repo.go`（76） — **精读重点**：PO ↔ 领域类型转换、`ListAll` 的 `created_at asc, id asc` 稳定排序

### 缓存（Redis）

- [ ] `internal/infrastructure/cache/redis/client.go`（46） — 连接与 `Ping`
- [ ] `internal/infrastructure/cache/redis/captcha.go`（61） — 验证码存储实现 `CaptchaStore`
- [ ] `internal/infrastructure/cache/redis/vector.go`（135） — **精读重点**：RediSearch 向量索引存储；账号级索引（`rag_docs:{accountNo}:idx`）+ 文档级 key 前缀，`DeleteDocVectors` 用 SCAN 分批按文档删除、`DeleteIndex` 清空知识库

### 安全 / 邮件 / 文档存储

- [ ] `internal/infrastructure/security/password.go`（34） — bcrypt 实现 `PasswordHasher`
- [ ] `internal/infrastructure/security/jwt.go`（67） — **精读重点**：JWT 签发/解析实现 `TokenIssuer`
- [ ] `internal/infrastructure/email/mailer.go`（52） — 验证码邮件实现 `Mailer`
- [ ] `internal/infrastructure/storage/docstorage.go`（174） — **精读重点**：`uploads/{account_no}` 目录约定，实现 `DocStorage`；多文档支持（`HasUserDocs`、`RemoveUserDoc` 按文档删除并防路径逃逸）

### RAG 引擎

- [ ] `internal/infrastructure/rag/parser.go`（151） — **精读重点**：按扩展名解析文档为纯文本（`.txt`/`.md` 直读、`.pdf` 抽取、`.docx` 解压 OOXML + 解析 `<w:t>`）
- [ ] `internal/infrastructure/rag/document.go`（128） — **精读重点**：`SplitIntoChunks` 按 rune 切块带重叠（防死循环/丢空块）、`LoadDocuments` 解析+分块（每块独立 `chunk_N` ID 与 `source`/`chunk` 元数据）、`BuildPrompt` 带来源拼装提示词
- [ ] `internal/infrastructure/rag/engine.go`（257） — **精读重点**：构造时复用 embedder、`NewEngine` 填充默认值、按账号建/删/检索向量、`FilterByDistance` 距离阈值过滤（RAG 路由基础）、TopK，实现领域 `rag.Indexer`

### 消息队列（RabbitMQ）

- [ ] `internal/infrastructure/mq/rabbitmq/transport.go`（98） — **精读重点**：纯传输层连接/发布/消费，`Publisher`（实现 `MessageSink`）、`Consumer`
- [ ] `internal/infrastructure/mq/rabbitmq/message.go`（77） — 消息序列化与消费回调如何经 `MessageRepository` 落库

### AI 模型适配器（本层最难，按 易→难 读）

- [ ] `internal/infrastructure/ai/schema.go`（24） — 领域 `Message` ↔ eino schema 互转
- [ ] `internal/infrastructure/ai/factory.go`（71） — **精读重点**：`modelType + 配置 → Model` 创建逻辑（含 RAG 的 API Key 与 query 改写开关传递）
- [ ] `internal/infrastructure/ai/openai.go`（82） — 最基础模型实现 `chat.Model`
- [ ] `internal/infrastructure/ai/ollama.go`（76） — 本地模型预留实现
- [ ] `internal/infrastructure/ai/rag.go`（175） — **精读重点**：RAG 如何在普通对话外层包检索增强；多轮 query 改写（`enableRewrite`）、RAG 路由（账号无文档或召回为空时跳过，不污染普通对话）
- [ ] `internal/infrastructure/ai/mcp.go`（268，全项目最复杂） — **精读重点**：MCP 工具调用链路、多轮工具交互

### TTS / 图像

- [ ] `internal/infrastructure/tts/baidu.go`（181） — **精读重点**：百度 TTS 鉴权与合成流程，实现 `Synthesizer`
- [ ] `internal/infrastructure/image/recognizer.go`（216） — **精读重点**：ONNX 模型加载、图像预处理、推理与标签映射，实现 `Recognizer`

---

## 阶段 7：组合根与入口（全局收束，需要前面全部铺垫）

- [ ] `config/config.go`（245） — **精读重点**：各配置段含义、哪些仍走环境变量；`[ragModelConfig]` 新增分块（`chunkSize`/`chunkOverlap`）、检索（`topK`/`maxDistance`）与多轮改写（`enableQueryRewrite`）参数，API Key 优先取 `[aiModelConfig]` 再回退环境变量
- [ ] `internal/bootstrap/app.go`（309，理解架构的关键） — **精读重点**：`New()` 自上而下装配（持久化 → 缓存 → RAG 引擎/AI 工厂 → MQ → 会话管理器 → 历史回放 → 消费者 → 其余适配器 → 应用服务 → 接口/路由）；注意 `NewEngine` 现返回 error 且在启动时一次性创建 embedder；`replayRecentSessions` 的一致性保证；`Shutdown` 按依赖反序释放资源
- [ ] `cmd/server/main.go`（39） — **精读重点**：极简入口仅做信号监听与调用 `bootstrap`，体会「入口很薄」的意义

> 读完阶段 7，回头再看一遍 `app.go`，此时每个被注入的组件你都已读过，应能完整画出依赖图。

---

## 阶段 8：独立模块 `cmd/mcp`（独立 Go module，可选）

- [ ] `cmd/mcp/main.go`（76） — MCP 服务/客户端入口
- [ ] `cmd/mcp/client/client.go`（132） — MCP 客户端
- [ ] `cmd/mcp/server/server.go`（186） — MCP 天气工具服务端

---

## 阶段 9：前端（可选）

- [ ] `vue-frontend/package.json` — 依赖与启动方式
- [ ] `vue-frontend/vue.config.js` — `/api` 代理规则
- [ ] 按页面/接口调用关系阅读 Vue 源码

---

## 精读时持续追踪的问题

- [ ] 一个请求：路由 → 中间件 → 控制器 → 应用服务 → 领域端口 → 基础设施实现，各步骤分别做了什么
- [ ] DTO、应用结果类型、领域实体/值对象、PO 四类对象分别给谁用、在哪里转换
- [ ] 领域层为什么零框架依赖（对照 `test/architecture_test.go`）
- [ ] 聊天链路中：消息历史、模型选择、消息持久化分别由谁负责（`Conversation` / `ModelFactory` / `MessageSink` + `MessageRepository`）
- [ ] 全局单例是如何被 `bootstrap` 的显式依赖注入取代的

能清楚回答以上 5 点，即说明已读透本项目主干。
