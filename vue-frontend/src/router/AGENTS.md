# AGENTS.md

## 模块职责

- 本目录维护前端路由表和路由守卫。

## 变更约束

- 继续用 `meta.requiresAuth` 驱动鉴权守卫，避免把登录校验散落到各个页面组件。
- 路径、重定向和页面命名改动前，先检查菜单导航、浏览器直达和未登录跳转是否仍然正常。

## 验证

- 路由改动后运行 `npm run lint`，并手动验证未登录访问受保护路由会被重定向到 `/login`。
