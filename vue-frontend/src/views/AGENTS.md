# AGENTS.md

## 模块职责

- 本目录实现页面级组件，包括登录、注册、菜单、AI 对话和图片识别视图。

## 变更约束

- 页面组件优先负责交互和展示，不要把公共请求拦截、路由守卫或接口契约改写复制到单个页面内部。
- 新页面或接口字段变化要同步确认后端 DTO、错误码和响应信封仍与页面读取方式匹配。
- 流式对话、文件上传和图片识别这类长链路交互，要保留清晰的加载态和失败提示。

## 验证

- 页面改动后运行 `npm run lint`，并至少手动检查受影响页面的主流程。
