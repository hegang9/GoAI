# AGENTS.md

## 模块职责

- 本目录定义用户实体、未找到错误和用户相关端口（仓储、密码哈希、令牌、验证码、邮件）。

## 变更约束

- 保持 `ErrNotFound` 作为统一领域语义，基础设施层负责把底层未命中错误翻译成它。
- `AccountNo`、`Email`、`Name` 的职责边界必须稳定：内部身份、登录邮箱、显示名不能互换。
- 不要在领域实体中夹带 HTTP 校验标签、GORM tag 或 Redis/SMTP 细节。

## 验证

- 修改后运行 `go test ./test/... -v`，并确认架构测试仍然通过。
