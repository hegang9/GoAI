# AGENTS.md

## 模块职责

- 本目录定义会话聚合、消息/会话实体、模型端口、消息持久化端口和会话管理器。

## 变更约束

- 保持 `Conversation` 只通过 `Model`、`MessageSink`、仓储端口协作，不要引入具体 AI SDK、MQ 或数据库类型。
- `Model.Type()` 与工厂约定的模型编号要稳定，避免打破上层路由和历史会话回放。
- `Manager` 继续负责内存会话复用与回放承接，不要把跨会话缓存逻辑散落到应用层。

## 验证

- 修改后运行 `go test ./test/... -v`，重点关注会话与回放相关测试。
