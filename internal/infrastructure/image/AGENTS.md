# AGENTS.md

## 模块职责

- 本目录实现 ONNX 图片识别适配器，负责模型会话、图像预处理和结果标签映射。

## 变更约束

- 保持模型资源管理可预测，新增缓存或并发优化时要确认 session 生命周期不会泄漏。
- 领域接口只需要识别结果文本；不要把 HTTP 上传、控制器或视图层概念混进识别器实现。

## 验证

- 图片识别链路改动后运行 `go test ./test/... -v`，必要时补人工识别冒烟。
