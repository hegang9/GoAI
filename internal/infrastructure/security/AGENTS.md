# AGENTS.md

## 模块职责

- 本目录实现密码哈希与 JWT 令牌适配器，为用户领域端口提供安全能力。

## 变更约束

- 保持 `JWTIssuer` 只关心签发与解析，不要把 Gin 中间件、HTTP Header 或控制器逻辑混进来。
- `account_no` 仍是 token 中的关键身份字段；改动 claims 结构前先检查中间件和前端登录链路。
- 密码哈希算法升级要兼顾兼容性与已有数据验证能力。

## 验证

- 安全链路改动后运行 `go test ./test/... -v`，并关注登录、鉴权和注册回归风险。
