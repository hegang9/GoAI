# AGENTS.md

## 模块职责

- 本目录对图片识别能力做薄编排，把 `domain/image.Recognizer` 暴露为应用服务。

## 变更约束

- 保持服务层轻量，不要把 ONNX 预处理、模型文件路径或 HTTP 上传细节下沉到这里。
- 失败路径统一映射到项目错误码，避免把底层识别器错误直接暴露给接口层。

## 验证

- 图片能力改动后运行 `go test ./test/... -v`，并确认相关接口仍能返回统一信封结构。
