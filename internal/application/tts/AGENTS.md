# AGENTS.md

## 模块职责

- 本目录编排文字转语音任务的创建与查询，依赖 `domain/tts.Synthesizer` 端口。

## 变更约束

- 保持“创建任务 / 查询任务”两阶段语义清晰，不要把具体百度 TTS 协议细节引入应用层。
- 返回值继续使用领域 `TaskResult` 与统一错误码，不要在此层拼装 HTTP 响应结构。

## 验证

- 修改后运行 `go test ./test/... -v`，并关注接口层是否仍按既有字段返回任务结果。
