# AGENTS.md

## 模块职责

- 接口层负责把 HTTP 请求映射到应用服务，把应用结果转换成对外响应。

## 分层约束

- 本层可以依赖应用层和少量领域端口（如 JWT 鉴权端口），但不要直接触碰基础设施实现或数据库连接。
- 控制器、DTO、路由、中间件、SSE 和通用 HTTP 工具继续分目录维护，避免职责交叉。

## 变更约束

- 对外契约变化必须同步检查前端 `vue-frontend`、README 和测试预期。
