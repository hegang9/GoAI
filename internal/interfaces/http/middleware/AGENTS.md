# AGENTS.md

## 模块职责

- 本目录实现 HTTP 中间件，当前重点是 JWT 鉴权与账号上下文注入。

## 变更约束

- 继续通过领域 `TokenIssuer` 解析 token，不要让中间件直接依赖 JWT 具体实现。
- 鉴权成功后注入到上下文中的账号编号字段必须保持稳定，否则会连锁影响聊天、文件和前端请求链路。

## 验证

- 鉴权逻辑改动后运行 `go test ./test/... -v`，并人工验证受保护路由的 401/200 场景。
