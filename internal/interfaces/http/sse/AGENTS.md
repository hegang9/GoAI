# AGENTS.md

## 模块职责

- 本目录封装 SSE 写出细节，为流式聊天提供统一的数据、会话 ID 和结束标记输出。

## 变更约束

- 保持 SSE 帧格式稳定，避免前端流式解析因字段名或结束信号变化而失效。
- 这里只处理流式传输协议，不负责聊天业务状态和模型生成逻辑。

## 验证

- 流式输出改动后运行 `go test ./test/... -v`，并人工验证聊天流式接口至少一轮。
