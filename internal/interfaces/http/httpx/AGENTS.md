# AGENTS.md

## 模块职责

- 本目录提供 HTTP 层通用绑定、包装器和统一响应输出工具。

## 变更约束

- `BindJSON`、`Handler`、`JSON` 的职责要保持通用；不要在这里塞入某个单独业务接口的分支。
- 统一信封结构和错误码映射是这里的核心契约，改动前必须同步检查前端 `src/utils/api.js` 的展平逻辑。

## 验证

- 变更后运行 `go test ./test/... -v`，并人工验证一个 JSON 绑定失败和一个成功响应场景。
