# GopherAI 架构优化方案

> 本文档分析 GopherAI 后端当前的分层与依赖问题，给出目标架构与分阶段、低风险的迁移方案。
>
> **实施状态（2026-06）：本方案已落地。** 项目已完成向「分层架构 + 端口与适配器」的整体重构：
> - `pkg/`：跨层通用工具（`logger`/`code`/`random`/`id`/`fileutil`/`hash`）。
> - `internal/domain/`：领域层，承载实体、值对象、聚合（如 `chat.Conversation`）与端口接口，无框架依赖。
> - `internal/application/`：应用层用例编排（`user`/`chat`/`file`/`image`/`tts`）。
> - `internal/infrastructure/`：端口的具体适配器（`persistence`/`cache/redis`/`mq/rabbitmq`/`ai`/`rag`/`security`/`email`/`image`/`tts`/`storage`）。
> - `internal/interfaces/http/`：接口层（`router`/`controller`/`dto`/`middleware`/`sse`/`httpx`）。
> - `internal/bootstrap/` + `cmd/server/`：组合根与入口，消除全局单例、改为显式依赖注入。
> - `common/mcp` 迁移至 `cmd/mcp`（独立 Go module）。
> - 领域层「零框架依赖」约束由 `test/architecture_test.go` 固化校验。
>
> 下文保留原方案分析与目标，作为设计依据与背景记录。

## 背景

GopherAI 是一个 Go + Gin 的 AI 应用后端，已经经历过多轮局部重构（`converter` 拆分、`utils` 整理、`common/aihelper` 拆包、`common/rag` 拆包、SSE 下沉、TTS 补 service 层、优雅关闭等，详见 `TODO.md`）。

这些重构都是在**现有分层**内部做的「拆文件、补空层」式优化，并没有解决两个更根本的结构问题：

1. **缺少真正的领域层（domain）**：业务实体、业务规则、聚合状态散落在 `model`（GORM）、`bo`（贫血结构体）和 `common/aihelper`（会话聚合）三处，没有一个统一承载业务对象与业务行为的层。
2. **`common` 里混入了业务模块**：`common/aihelper`、`common/rag`、`common/image`、`common/tts` 是业务能力，却和 `mysql`/`redis`/`rabbitmq`/`logger` 这类纯技术基础设施放在同一个 `common` 包下，导致「基础设施」与「业务」边界模糊。

## 目标

- 引入清晰的领域层，让业务实体与业务规则有明确归属，与持久化模型（`model`）和接口模型（`dto`）解耦。
- 把 `common` 收敛为**纯技术基础设施**，将业务能力（AI 会话、RAG、图片识别、TTS）迁出到领域 / 应用层。
- 通过「端口与适配器」（接口反转）消除现存的反向依赖与跨层耦合，让依赖方向单向收敛。
- 提供**分阶段、可独立验证、可随时回滚**的迁移路径，避免一次性大爆炸式重构。
- 不破坏对外 HTTP 契约（`docs/API.md` 中的接口行为保持不变）。

## 现状分析

### 当前分层

```text
router/        路由注册（HTTP 入口）
controller/    控制器：绑定 DTO、调用 service、封装响应、SSE 适配
service/       业务编排：user / session / image / tts / file
dao/           数据访问：user / session / message
model/         GORM 数据库模型：user / session / message
dto/           接口层传输对象（请求/响应）
bo/            业务对象（service 返回值）
converter/     bo <-> dto 转换
mapper/        model.Message <-> eino schema.Message 转换
auth/          密码哈希 + JWT
random/ id/ fileutil/ utils/   通用小工具
middleware/jwt 鉴权中间件
common/        mysql redis rabbitmq email logger code image tts mcp rag aihelper
config/        配置加载（全局单例）
```

主链路（聊天）大致是：

```text
router -> controller/session.go -> service/session -> common/aihelper(manager/factory/session/provider)
                                                    -> dao(经 rabbitmq 异步) -> model -> MySQL
```

### 核心问题

#### 问题 1：没有领域层，业务对象三处分裂

同一个业务概念被拆成三种互不相关的结构：

- `model.Message` / `model.Session` / `model.User`：GORM 持久化模型，带 `gorm` tag，面向数据库。
- `bo.SessionInfoBO` / `bo.AIResponseBO` / `bo.MessageBO`：贫血结构体，只有字段、没有行为，实际只承担「service 输出 DTO」的角色（见 `bo/session.go`）。
- `common/aihelper/session.AIHelper`：**真正的业务聚合**——它持有会话消息历史、并发锁、持久化回调、模型实例，并实现「追加消息 / 生成回复 / 流式生成」等业务行为（见 `common/aihelper/session/helper.go`）。

结果是：最有业务含义的「会话聚合」反而藏在 `common` 里，而名为业务对象的 `bo` 却是贫血的传输结构。没有一个层来回答「会话/消息/用户这些领域对象到底是什么、有哪些不变量和行为」。

#### 问题 2：`common` 混入业务能力

`common` 目录同时包含两类性质完全不同的包：

- 纯技术基础设施：`mysql`、`redis`、`rabbitmq`、`email`、`logger`、`code`。
- 业务能力：`aihelper`（AI 会话编排）、`rag`（检索增强）、`image`（图片识别）、`tts`（语音合成）、`mcp`（工具协议）。

把业务能力当作「公共组件」放进 `common`，会让任何人都误以为它们是可随意复用的底层工具，实际上它们承载的是产品业务逻辑。

#### 问题 3：跨层 / 反向依赖（依赖方向被打破）

代码核对后确认存在以下「下层反向依赖上层」或「基础设施依赖业务」的情况：

- `common/aihelper/session/helper.go` 直接 `import` 了 `GopherAI/mapper`、`GopherAI/model`、`GopherAI/common/rabbitmq`：一个挂在 `common` 下的「基础设施」包，却直接耦合了持久化模型、领域转换和 MQ 发送。
- `common/rabbitmq/meesage.go` 直接 `import` 了 `GopherAI/dao` 和 `GopherAI/model`：传输层（MQ）里写死了「消费消息就落库」的业务逻辑。
- `common/mysql/mysql.go` `import` 了 `GopherAI/model` 用于 `AutoMigrate`：基础设施反向依赖领域模型。
- `middleware/jwt/jwt.go` `import` 了 `GopherAI/controller` 并使用 `controller.Response`：中间件反向依赖控制器（`TODO.md` 已记录）。
- `common/aihelper/provider/rag.go` `import` 了 `GopherAI/common/rag` 与 `config`：provider 之间、provider 与配置之间横向耦合。

#### 问题 4：全局单例与隐式状态

- AI 会话：`manager.GetGlobalManager()`、`factory.GetGlobalFactory()`（`sync.Once` 单例）。
- 基础设施：`mysql.DB`、`redis.Rdb`、`rabbitmq.RMQMessage` 包级全局变量。
- 配置：`config.GetConfig()` 几乎被所有 `common` 与业务包导入。

全局单例让依赖关系隐式化，单元测试难以替身（mock），也难以做按需初始化与多实例隔离。

#### 问题 5：历史遗留与重复

- `common/aihelper/exports.go` 是上一轮拆包遗留的兼容门面（type alias + var 转发），增加一层无业务含义的间接。
- `converter`（bo↔dto）与 `mapper`（model↔schema）两套转换包，命名与归属不统一。
- `utils` 仅剩 `MD5`，`converter/converter.go` 留有大段注释掉的 `UserBO` 转换代码。

## 目标架构

采用「分层 + 端口与适配器（Hexagonal / Ports & Adapters）」的务实组合，依赖方向**始终指向领域层**。

### 目标分层与依赖方向

```text
                +------------------------------------------+
   入站适配器    |  interfaces (HTTP)                       |
   (driving)    |  router / controller / dto / middleware  |
                +---------------------+--------------------+
                                      | 调用 用例
                                      v
                +------------------------------------------+
   应用层        |  application (用例编排 / 事务边界)         |
                |  user / chat / rag / image / tts 用例     |
                +---------------------+--------------------+
                                      | 依赖 领域接口(ports)
                                      v
                +------------------------------------------+
   领域层 (核心) |  domain                                   |
                |  实体/值对象/聚合 + 领域服务 + 端口接口     |
                |  user / chat(session+message+helper) /    |
                |  rag / image / tts                        |
                +---------------------+--------------------+
                                      ^ 实现 领域接口
                                      |
                +---------------------+--------------------+
   出站适配器    |  infrastructure (技术实现)                |
   (driven)     |  mysql redis rabbitmq email logger        |
                |  aiprovider(openai/ollama/mcp) rag-store  |
                +------------------------------------------+
```

依赖规则（单向，编译期可校验）：

- `interfaces` 只依赖 `application` 和 `domain`（用其类型/接口），不依赖 `infrastructure` 具体实现。
- `application` 只依赖 `domain`（端口接口），编排用例、定义事务边界。
- `domain` **不依赖任何外层**，不 `import` gin、gorm、rabbitmq、redis、config 等；只定义实体与端口接口。
- `infrastructure` 依赖 `domain`（实现其端口接口），可依赖第三方库与 `config`。
- 依赖注入在 `bootstrap`（或 `cmd/server`）层完成：构造 infra 实现，注入 application/domain。

### 目标目录结构（建议）

保留 Go 习惯，可放在 `internal/` 下以收紧可见性。命名可按团队偏好微调：

```text
cmd/server/main.go                 启动入口（精简，仅 bootstrap + 信号处理）
internal/
  bootstrap/                       依赖装配、生命周期、迁移、消息回放
  domain/
    chat/                          会话聚合：Session / Message / 会话历史行为 + 端口接口
      entity.go                    领域实体与值对象（无 gorm/json tag）
      repository.go                MessageRepository / SessionRepository 接口
      model.go (port)              AIModel 接口（原 provider.AIModel 上移为领域端口）
      service.go                   会话领域服务（追加消息、生成回复的纯业务规则）
    user/                          用户实体 + 仓储接口 + 密码/认证端口
    rag/                           RAG 领域接口（检索器、索引器端口）
    image/  tts/                   各自领域接口
  application/
    chat/  user/  rag/  image/ tts/   用例编排（原 service/* 上移并去除 HTTP/infra 细节）
  interfaces/http/
    router/ controller/ dto/ middleware/ sse/   入站适配（原 router+controller+dto+middleware）
  infrastructure/
    persistence/mysql/             GORM 仓储实现（实现 domain 仓储接口）
    cache/redis/
    mq/rabbitmq/                   transport-only，落库逻辑移交 application
    ai/openai/ ollama/ mcp/        实现 domain.chat.AIModel 端口
    rag/                           embedding/index/retriever/store 实现 domain.rag 端口
    image/ tts/ email/             外部服务适配
    logger/ code/                  纯技术能力（可保留为 pkg）
pkg/ (可选)                        auth random id fileutil 等真正通用、无业务的工具
config/                            配置定义（由 bootstrap 读取后注入，不再被各层直接 import）
```

> 说明：是否引入 `internal/` 与 `cmd/` 由团队决定；最小改动版本也可保留现有根目录平铺风格，只新增 `domain/`、`application/`、把 `common` 拆成 `infrastructure/` + `pkg/`。本方案后续阶段以「能渐进迁移」为准。

### 关键设计

#### 1. 把 `AIHelper` 还原为领域聚合

当前 `common/aihelper/session.AIHelper` 实质是「会话聚合」，但它直接依赖 `rabbitmq`（持久化副作用）和 `mapper`/`model`。目标：

- 聚合本身（消息历史、并发安全、追加/生成/流式行为）上移到 `domain/chat`。
- 「调用哪个模型」通过领域端口 `AIModel` 接口注入（接口定义在 domain，实现在 `infrastructure/ai`）。
- 「消息如何持久化」通过领域端口 `MessageRepository`（或领域事件 + application 订阅）注入，**聚合不再直接 import rabbitmq**。MQ 异步落库变成 application/infra 的实现细节。

#### 2. 端口与适配器（接口反转）

- `domain` 定义接口：`MessageRepository`、`SessionRepository`、`UserRepository`、`AIModel`、`RAGRetriever`、`ImageRecognizer`、`TTSClient`、`Mailer`、`TokenIssuer` 等。
- `infrastructure` 提供实现：`mysqlMessageRepo`、`openAIModel`、`redisRAGRetriever`…
- 由此消除问题 3 的全部反向依赖：`mysql` 不再 import `model`（改为实现仓储接口）、`rabbitmq` 不再 import `dao`、`aihelper` 不再 import `rabbitmq`/`mapper`。

#### 3. 依赖注入与 bootstrap

- 新增 `internal/bootstrap`：负责按顺序构造 logger、config、mysql、redis、rabbitmq，组装各 repository/adapter，再注入 application 与 domain 服务，最后交给 HTTP server。
- 逐步用「构造时注入」替代 `GetGlobalXxx()` 全局单例，提升可测试性（对应 `TODO.md` 中「收缩全局单例」「精简 main.go」两项）。
- 把 `main.go` 里的消息回放、AI 状态恢复迁入 bootstrap。

#### 4. 模型转换归位

- `model <-> domain entity` 转换：放到 `infrastructure/persistence`（仓储实现内部）。
- `domain entity / 用例结果 <-> dto` 转换：放到 `interfaces/http`（原 `converter` 角色）。
- `domain message <-> eino schema.Message` 转换：放到 `infrastructure/ai`（原 `mapper` 角色，仅 AI 适配器需要）。
- 三类转换各自归位到相邻层，取消独立的顶层 `converter`/`mapper` 包（或保留为 interfaces/infra 子包）。

#### 5. 修复 middleware 反向依赖

- 把 `controller.Response` 提到独立的 `interfaces/http/httpx`（或 `dto`）公共响应包，`middleware/jwt` 改为依赖该公共包，断开 `middleware -> controller`。鉴权产出的 `accountNo` 仍通过 gin context 透传。

## 迁移计划

遵循「每一步都能独立编译、独立验证、可回滚」的原则，分阶段推进。前期以「低风险、断依赖」为主，后期才动主链路聚合。

### 阶段 0：准备与护栏

- 补充 / 完善架构依赖测试（扩展现有 `test/architecture_layout_test.go`），用 import 校验固化「禁止 domain 依赖外层」「禁止 middleware 依赖 controller」等规则，作为重构护栏。
- 现有对外接口行为冻结：以 `docs/API.md` 为基准，确保重构前后响应不变。

### 阶段 1：低风险断依赖（不挪聚合）

- 修复 `middleware/jwt -> controller` 反向依赖（提取公共响应包）。
- `common/rabbitmq`：抽出落库回调，改为 transport-only；消费侧业务（调用 `dao.CreateMessage`）上移到 application/consumer，并改手动 ACK（对应 `TODO.md` P2）。
- 清理历史遗留：评估并移除 `common/aihelper/exports.go` 兼容门面；删除 `converter` 注释死代码；收敛 `utils`。

### 阶段 2：引入领域层骨架

- 新建 `domain/`，定义 chat / user / rag / image / tts 的实体与端口接口（先定义、暂不挪实现）。
- 把 `provider.AIModel` 接口上移为 `domain/chat` 端口；`provider` 具体实现暂留原处但改为实现该端口。
- 用 `bo` 的字段为基础，沉淀为真正的领域实体（带必要的不变量校验）。

### 阶段 3：迁移业务能力出 `common`

- 将 `common/aihelper` 的聚合逻辑迁入 `domain/chat`，provider 实现迁入 `infrastructure/ai`；聚合改为依赖注入 `AIModel` + `MessageRepository`，移除对 `rabbitmq`/`mapper` 的直接依赖。
- 将 `common/rag`、`common/image`、`common/tts` 拆为「domain 端口 + infrastructure 实现」。
- `common` 仅保留纯基础设施（`mysql`/`redis`/`rabbitmq`/`email`/`logger`/`code`），并整体规划重命名为 `infrastructure`/`pkg`。

### 阶段 4：仓储化与依赖注入

- `dao` 改造为 `infrastructure/persistence` 下、实现 `domain` 仓储接口的适配器；`mysql` 不再 import `model`（迁移逻辑移交 bootstrap）。
- 新建 `bootstrap`，集中装配依赖，逐步替换 `GetGlobalManager`/`GetGlobalFactory` 等单例；精简 `main.go`。
- `service/*` 上移为 `application/*`，去除残留的 HTTP/infra 细节。

### 阶段 5：收尾

- 转换逻辑归位（model↔entity↔dto↔schema）。
- 同步更新 `README.md`、`阅读顺序.md`、`docs/`，并据本方案逐条勾掉 `TODO.md` 中相关条目。

## 风险与限制

- **改动面大**：阶段 3、4 触及聊天主链路与持久化，是最高风险区，需配合接口回归与（建议补充的）集成测试。
- **行为一致性**：消息异步落库、启动消息回放（`save=false`）的一致性语义必须保持，迁移时重点回归（参考 `TODO.md` 已核查结论）。
- **目录大改的协作成本**：引入 `internal/`、`cmd/`、整包重命名会产生大量 import 变更与潜在合并冲突，建议按阶段单独提交、分别评审。
- **范围边界**：本方案聚焦后端分层与依赖治理，不涵盖前端、不改变对外 API 契约；安全/可观测性等改进沿用 `TODO.md` 既有规划，不在本方案重复展开。
- **务实取舍**：是否落地完整 `internal/cmd/` 结构可按团队偏好裁剪，最小可行版本只需「新增 domain + application，拆分 common 为 infra/pkg，断反向依赖」即可获得大部分收益。

## 验证方式

- **编译**：每阶段结束 `go build ./...` 通过。
- **依赖护栏**：架构依赖测试（import 校验）通过，确保领域层零外部依赖、无反向依赖。
- **回归**：`go test ./...` 通过；对 user / chat（含 SSE 流式）/ rag / image / tts 主链路做接口回归，对照 `docs/API.md` 行为不变。
- **可回滚**：每阶段为独立提交，出现问题可单独回退该阶段。

## 待确认事项

请评审时重点确认以下取舍，确定后再进入执行：

1. 目录风格：采用 `internal/ + cmd/` 完整结构，还是保留根目录平铺、仅新增 `domain`/`application` 并把 `common` 拆为 `infra`/`pkg`。
2. 命名：`application` vs 继续叫 `service`；`infrastructure` vs 继续叫 `common`/`infra`。
3. 持久化解耦方式：消息落库走「领域事件 + application 订阅」还是「直接注入仓储 + application 编排 MQ」。
4. 迁移节奏：是否一次性推进到阶段 4，还是先合入阶段 0~2（低风险）观察后再继续。
